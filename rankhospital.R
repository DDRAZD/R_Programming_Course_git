

rankhospital <- function(state, outcome, num = "best") {
        ## Read outcome data
        
        
        
        ## Check that state and outcome are valid
        
        ##The function should check the validity of its arguments. If an invalid 
        ##state value is passed to rankhospital, the function should throw an 
        ##error via the stop function with the exact message "invalid state".
        ##If an invalid outcome value is passed to rankhospital, the function 
        ##should throw an error via the stop function with the exact message
        ##"invalid outcome" Return hospital name in that state with the given rank
        ## 30-day death rate
}
